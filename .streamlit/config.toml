[theme]
primaryColor = "#6d4c41"          # Ruya brown – affects slider active track, handle, etc.
backgroundColor = "#fff7ec"       # Main background
secondaryBackgroundColor = "#fff7ec"  # Widgets/dropdowns background (fixes dark selectbox list)
textColor = "#3e2723"             # Brown text
font = "sans serif"
